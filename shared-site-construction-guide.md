# 新サイト構築 実践手順ガイド

**作成日：2026年7月9日**
**参照元：`trip-matomeblog-analysis.md`（サイト分析）、`trip-matomeblog_spec.md`（技術仕様）**

このドキュメントは、trip-matomeblog.comの構築・運用から得た知見をもとに、**新しいテーマでサイトを立ち上げる際に、実際に何をどの順番で行うか**をまとめた実践手順書です。`multi-site-playbook.md`のチェックリストを、より具体的な作業レベルに落とし込んだものです。

---

## 📌 関連ドキュメントとの関係

trip-matomeblogに関する資料は、現在以下の4点で構成されています。それぞれの役割と参照関係は次の通りです。

```
trip-matomeblog-analysis.md（分析）
        ↓ この分析結果をもとに手順化
site-construction-guide.md（本ドキュメント：実践手順）
        ↓                              ↓
        ↓（詳細技術を参照）              ↓（汎用の型を参照）
trip-matomeblog_spec.md          multi-site-playbook.md
（このサイト固有の技術仕様）        （型と設計判断集・汎用）
        ↓                              ↑
        └────── 新サイト構築 ───────────┘
                （4点セットを参照して着手）
                        │
                        └─ 運用後、得られた教訓を
                           multi-site-playbook.md に還元
```

| ドキュメント | 役割 | 主な使いどころ |
|---|---|---|
| `trip-matomeblog-analysis.md` | trip-matomeblogが「なぜ・どう」作られたかの分析 | 新サイト着手前に、設計思想を理解したいとき |
| `site-construction-guide.md`（本書） | 分析を踏まえた、実際の作業手順（本ドキュメント） | 新サイト構築時、手を動かしながら参照する実務書 |
| `trip-matomeblog_spec.md` | trip-matomeblog固有の詳細な技術仕様 | 実装の詳細（関数名・判定ロジック等）を確認したいとき |
| `multi-site-playbook.md` | 複数サイト共通の「型」と、未確定の「設計判断集」 | どのファイルをそのまま移植でき、何を再設計すべきか確認したいとき |

**運用の流れ**：新サイトを構築する際は、本ドキュメント（手順）を軸にしながら、詳細が必要な箇所は`spec`（このサイト固有の実装）または`playbook`（汎用の型）を参照します。新サイト運用で得られた知見（例：新しい教訓、より良いテスト手順等）は、`multi-site-playbook.md`に還元し、次のサイト構築に活かします。この還流が、複数サイト展開全体の質を上げていく仕組みです。

---

## 📑 目次

1. [着手前に決めておくこと（設計フェーズ）](#1-着手前に決めておくこと設計フェーズ)
2. [基盤構築（環境フェーズ）](#2-基盤構築環境フェーズ)
3. [コードの移植とカスタマイズ（実装フェーズ）](#3-コードの移植とカスタマイズ実装フェーズ)
4. [軽量テスト（検証フェーズ）](#4-軽量テスト検証フェーズ)
5. [本番投入と初期運用（立ち上げフェーズ）](#5-本番投入と初期運用立ち上げフェーズ)
6. [運用データを見た調整（定着フェーズ）](#6-運用データを見た調整定着フェーズ)
7. [フェーズ別の所要時間の目安](#7-フェーズ別の所要時間の目安)
8. [サイト運営時のコスト感](#8-サイト運営時のコスト感)

---

## 1. 着手前に決めておくこと（設計フェーズ）

trip-matomeblogの分析で分かった通り、**コード（型）は使い回せても、判定基準・文体・提携先といった「中身」はテーマごとに作り直しが必要**です。ここを曖昧なまま実装に入ると、後から`criteria.json`やSYSTEM_PROMPTを何度も書き直す羽目になります。着手前に、以下を紙またはドキュメントに書き出してください。

### 1.1 サイトコンセプトを1文で決める

trip-matomeblogの例：「今まで頑張ってきたね。これからは遊ぼう！」

- 誰に、何を、どんな気持ちで届けるサイトか、1文で言い切れるまで詰める
- ここが曖昧だと、後述のSYSTEM_PROMPT（文体指示）が書けない

### 1.2 読者ターゲットを「価格帯」まで具体化する

trip-matomeblogは「40代後半〜65歳、宿泊1.5〜5万円/泊、食事〜1万円程度」まで数値化しています。これは`criteria.json`の判定基準に直結するため、以下を必ず決めてください。

- 対象年代（幅を持たせつつ、メイン層を明記）
- 金銭感覚の基準（サイトのジャンルに応じた単位で。物販サイトなら商品単価帯、サービス系なら利用料金帯等）
- 「歓迎する行動・体験」の具体例を5〜10個
- 「歓迎しない行動・体験」の具体例を5〜10個

この作業は、後で`criteria.json`の`travel_type_ok` / `travel_type_exclude`相当のリストにそのまま転記できる形で書いておくと、実装がスムーズです。

### 1.3 カテゴリー構成を決める

trip-matomeblogは9カテゴリー固定です。ただし「なぜ9個か」は言語化されていないため、新サイトでは以下の考え方で決めることを推奨します。

- 想定する情報源（RSSフィード等）のジャンル分布を先に把握する
- 1カテゴリーあたり、最低でも月数記事は投稿できる見込みがあるかを確認する（少なすぎるカテゴリーはページが寂しくなる）
- 6〜10個程度を目安に、多すぎず少なすぎない構成にする

### 1.4 画像取得の見通しを立てる

trip-matomeblogは無料API（Wikimedia/Pixabay）で運用していますが、日本語のニッチなキーワード（温泉旅館名等）では画像がヒットしにくいという弱点があります。新サイトのテーマについて、以下を事前に考えてください。

- 扱う被写体は一般的（風景・食べ物・乗り物等、世界共通で写真が多いもの）か、ニッチ（特定の日本文化・専門分野等）か
- 一般的な被写体が中心なら無料APIのままで様子見、ニッチな被写体が中心なら最初から有料サービスの検討も視野に入れる

### 1.5 収益化・アフィリエイト提携先を選定する

- サイトテーマに合うASP（アフィリエイトサービスプロバイダ）を2〜3社リストアップ
- 提携申請には審査期間があるため、**この段階で申請だけ先に済ませておく**（trip-matomeblogでも、ホットペッパーグルメの審査に時間がかかった実績あり）

---

## 2. 基盤構築（環境フェーズ）

設計が固まったら、実際の環境構築に入ります。

### 2.1 チェックリスト

- [ ] ドメイン取得
- [ ] レンタルサーバー契約・WordPress設置
- [ ] WordPressの初期設定（パーマリンク設定、テーマ選定等）
- [ ] Application Password発行（WP管理画面 > ユーザー > プロフィール > アプリケーションパスワード）
- [ ] GitHubリポジトリ作成（trip-blogリポジトリをテンプレートとしてコピー、または`git clone`後に`.git`履歴を削除して新規リポジトリ化）。②の方法を選ぶ場合、削除前に過去のコミット履歴にAPIキー等の機微情報が含まれていないか確認すること。①のテンプレート使用ではこの懸念は生じない。
- [ ] GitHub Secretsに以下を登録
  - `ANTHROPIC_API_KEY`
  - `PIXABAY_API_KEY`
  - `WP_BASE_URL`
  - `WP_USERNAME`
  - `WP_APP_PASSWORD`
  - `WP_CATEGORY_IDS`
  - `GOOGLE_SERVICE_ACCOUNT_JSON`（Search Console連携する場合）
- [ ] `.github/workflows/*.yml`の`permissions: contents: write`が明記されているか確認（欠けているとpushが403エラーになる、trip-matomeblogでの既知トラブル）

### 2.2 注意点

- Application Passwordは発行時にしか全文表示されないため、発行直後に必ずGitHub Secretsへ登録する
- `.htaccess`のWordPress標準リライトルールは、パーマリンク設定変更等で欠落することがある（trip-matomeblogで実際に発生した重大障害）。設置直後に一度、REST APIが正常に動くか（例：`https://ドメイン/index.php?rest_route=/wp/v2/posts`にアクセスして応答があるか）確認しておく

---

## 3. コードの移植とカスタマイズ（実装フェーズ）

### 3.1 そのまま移植するファイル（変更不要、または軽微な調整のみ）

| ファイル | 調整内容 |
|---|---|
| `images.py` | 基本的に変更不要。画像取得方式を有料サービスに変える場合のみ改修 |
| `criteria.py` | 変更不要（ロジックのみのため） |
| `wordpress.py` | 変更不要 |
| `dedup.py` | 変更不要 |
| `config.py` | 環境変数名が変わる場合のみ調整 |

### 3.2 中身を作り直すファイル

| ファイル | 何を書き換えるか |
|---|---|
| `criteria.json` | 1.2で決めた年代・価格帯・OK/NGリストに全面差し替え |
| `summarizer.py`のSYSTEM_PROMPT | 1.1のコンセプト・文体に合わせて全面書き換え。`category`のLiteral型リストは1.3のカテゴリーに差し替え |
| `article_generator.py`のSYSTEM_PROMPT | 体験記事を扱うサイトの場合、同様に文体を再定義 |
| `affiliate.py` | 1.5で選定した提携先に応じてリンク定数・分岐ロジックを差し替え |
| `feeds.yaml` | 新テーマに合うRSSフィードを収集して登録 |

### 3.3 実装の進め方（推奨順序）

1. **`criteria.json`を先に書く**：判定基準が固まっていないと、SYSTEM_PROMPTの整合性が取れないため、必ず先に着手する
2. **`summarizer.py`のSYSTEM_PROMPTを書く**：`criteria.json`の内容を`build_criteria_block()`経由で埋め込む構造は変えず、文体部分だけ書き換える
3. **`category`のLiteral型を新カテゴリーに差し替える**：`categoryフィールドの技術的強制`（trip-matomeblog仕様書3.4参照）は必ず踏襲する。ここを`str`型に戻すと、同じ不具合を再現するリスクがある
4. **`affiliate.py`の分岐ロジックを調整する**：判定軸（`article_type`, `lodging_link_suitable`等）の構造は保ちつつ、リンク先だけ差し替える
5. **`feeds.yaml`を作る**：最初は5〜10本程度の少数フィードで様子を見て、運用しながら増やす（4.3節参照）

---

## 4. 軽量テスト（検証フェーズ）

trip-matomeblogでは、開発初期に本番ワークフロー全体を何度も試行錯誤で実行し、通常の数倍〜十数倍のAPIコストがかかる失敗をしました。新サイトでは、最初からこの手順を徹底してください。

### 4.1 テストの基本思想

- **品質の維持・向上**と**コストの抑制**を両立させる
- 確認したい範囲だけを狙い撃ちでテストし、無関係な処理でAPIを消費しない

### 4.2 手順

1. **記事判定ロジックのみのテスト**（画像取得は呼ばない）
   - `summarize()`のみを呼ぶ軽量スクリプトを用意（trip-matomeblogの`test_summarize_only.py`を参考に、新サイト用のダミー記事データで作成）
   - `article_type`・`senior_suitable`・`category`・`image_query`が意図通り出力されるか、1〜2記事で確認する
2. **画像取得ロジックのみのテスト**（記事要約は呼ばない）
   - 決め打ちのキーワードで`fetch_related_photos()`のみを呼ぶ軽量スクリプトを用意（`test_image_only.py`を参考に作成）
   - 新テーマ特有のキーワード（例：グルメサイトなら「ラーメン」「和菓子」等）で、実際にどれくらい画像がヒットするか確認する
3. **上記2つで問題なければ、ワークフローを`draft`設定で1回だけ実行**
   - WordPress側で下書きとして生成される記事を目視確認（アイキャッチ画像、本文の文体、リンクの出方等）
4. **問題なければ`publish`設定に変更し、本番運用を開始**

### 4.3 このフェーズで特に確認すべきポイント（trip-matomeblogでの反省を踏まえて）

- `image_query`は1語で生成されているか（2語以上だと画像がヒットしにくい、trip-matomeblog仕様書3.4参照）
- `category`が想定した候補内に収まっているか（Literal型が効いているか）
- 画像のブラックリスト登録が「危険」の場合のみ発生し、「単に関連性が低いだけ」の画像を誤って除外していないか

---

## 5. 本番投入と初期運用（立ち上げフェーズ）

### 5.1 立ち上げ直後にやること

- [ ] 自動投稿ワークフローのスケジュール設定（cron）を確認し、想定通りの時刻に実行されるか2〜3日様子を見る
- [ ] Google Search Consoleへのサイト登録
- [ ] noindex設定（カテゴリー/タグ/アーカイブページ、trip-matomeblogのSEO運用を踏襲）
- [ ] ASPの提携申請状況を定期確認（審査中のものがあれば）

### 5.2 「見た目の空白期間」対策

新サイト立ち上げ直後は、記事数もアクセスもゼロからのスタートになるため、trip-matomeblogのような「画像なし記事が並ぶと見栄えが悪い」問題が起きやすいタイミングでもあります。以下を意識してください。

- 自動投稿が安定するまでは、こまめにWordPress管理画面で生成記事の見た目を確認する
- 画像取得に問題が出た場合、無理に本番投稿を続けず、一時的に`draft`設定に戻して原因を調査する判断も検討する

---

## 6. 運用データを見た調整（定着フェーズ）

立ち上げから1〜2週間経ったら、以下の観点でデータを見て調整します。

### 6.1 確認する指標

- カテゴリーごとの記事数の偏り（極端に少ないカテゴリーがあれば、フィード追加やカテゴリー統合を検討）
- `senior_suitable=False`（またはそれに相当する読者不適合判定）でスキップされた記事の割合（高すぎる場合、フィード選定またはcriteria.jsonの基準が厳しすぎる可能性）
- 画像取得の成功率（WordPress管理画面の「Without featured image」フィルタで確認）
- 実際のアクセス数・人気記事の傾向

### 6.2 調整の進め方

- `criteria.json`はデータのみの変更で済むため、気軽に調整して良い（コード変更不要という設計を活かす）
- SYSTEM_PROMPTの文体調整は、生成された記事を10本程度読んでから判断する（1〜2本だけでは判断材料として不足）
- 得られた知見は、`multi-site-playbook.md`の該当セクションに追記し、次のサイト構築に活かす

---

## 7. フェーズ別の所要時間の目安

trip-matomeblogでの実績（あくまで参考値、今後のサイトで変わりうる）：

| フェーズ | 目安時間 |
|---|---|
| 1. 設計フェーズ | 半日〜1日（コンセプト・ターゲット・カテゴリーを詰める） |
| 2. 基盤構築 | 半日程度（ドメイン取得等、外部要因で前後する） |
| 3. 実装フェーズ | 1〜2日（criteria.json・SYSTEM_PROMPT・アフィリエイト設定） |
| 4. 検証フェーズ | 半日〜1日（軽量テスト→draft確認） |
| 5. 立ち上げフェーズ | 初動3日程度は経過観察 |
| 6. 定着フェーズ | 継続的（1〜2週間サイクルで見直し） |

---

## 8. サイト運営時のコスト感

新サイトを立ち上げる前に、月々どの程度のコストがかかるかを把握しておく必要があります。trip-matomeblogでの実績をもとに整理します。

### 8.1 固定費（サイト基盤）

| 項目 | 目安 | 備考 |
|---|---|---|
| ドメイン | 年額1,000〜3,000円程度 | サイトあたり |
| レンタルサーバー | 月額1,000〜3,000円程度 | プランによる。複数サイトを1契約でまとめられる場合あり |
| WordPressテーマ・プラグイン | 無料〜数千円（買い切り） | 必須ではない |

### 8.2 変動費（Claude API利用料）

**これが最も見積もりにくく、かつ最も重要な項目です。**

trip-matomeblogでの実績値（Haikuモデル使用）：

- **通常運用時（1日1回の自動投稿、10記事/日）**：月あたり数百円〜1,000円程度の水準で推移（2026年7月の実績グラフより）
- **開発・デバッグ時（本番ワークフローを繰り返しテスト）**：1日で通常の数倍〜十数倍のトークンを消費することがある（実例：2026/7/6〜7/7、通常月間数百円ペースのところ、1日で約$5〜9消費）

**コストが跳ね上がる典型的な原因**：
1. 本番相当のワークフロー（数十件の候補記事を処理）を、コード修正のたびに何度も実行してしまう
2. 画像取得のフォールバック検索が、ヒットしない記事に対して何度もAPIを呼び続ける（上限を設けていない場合）
3. 記事要約と画像判定を毎回セットでテストし、片方だけ確認したい場合でも両方のAPIを消費してしまう

**対策**（4章の軽量テスト運用を徹底することが直結）：
- コード変更の確認は1〜2記事の軽量テストに絞る
- フォールバック検索の試行回数に上限を設ける（trip-matomeblogでは`MAX_FALLBACK_ATTEMPTS=4`を設定）
- 本番ワークフロー全体の実行は「最終確認」の1回に限定する

### 8.3 複数サイト運用時の注意点

- Claude APIの利用枠・請求は、**サイトごとではなくAnthropicアカウント単位**で合算されます。サイト数が増えるほど、1サイトのコスト管理の甘さが全体に波及します
- 画像取得API（Wikimedia/Pixabayは無料）についても、有料サービスへ移行する場合は、サイト数分の契約が必要になるか、上位プランで複数サイト分をまとめられるかをサービスごとに確認する必要があります（4.4節参照）
- 決済トラブル（カード会社側のセキュリティブロック等）が起きた場合、**影響は全サイトの自動投稿が同時に停止する**形で及びます。運営サイト数が増えるほど、決済手段の安定性の重要度が上がります

### 8.4 収益化の見通しとの比較

trip-matomeblogは月100万円のアフィリエイト収益を目標に運営されています。上記の運営コスト（月数千円〜1万円程度、サーバー代含む）は、この目標に対して十分に小さい水準です。ただし、**サイト初期は収益がほぼゼロの期間が続く**ため、「コストをかけてでも急ぐ」のではなく、「無料APIや軽量テストでコストを抑えながら、収益が立ち上がるまで運営を継続する」という設計思想が、trip-matomeblogの現在の運営方針とも一致しています。

新サイトを検討する際も、**最初は低コスト構成（無料画像API、Haikuモデル、軽量テスト運用）で立ち上げ、収益が見えてきた段階で有料サービスへの投資を検討する**、という順序がリスクを抑えられます。

---

## 関連ドキュメント

- `trip-matomeblog_spec.md`：trip-matomeblog固有の技術仕様（詳細な実装レベルの参照先）
- `multi-site-playbook.md`：複数サイト展開のための「型」と「設計判断集」
- `trip-matomeblog-analysis.md`：trip-matomeblogの作られ方の分析（本ドキュメントの背景資料）

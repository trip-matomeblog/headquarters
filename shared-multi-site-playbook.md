# 多サイト展開プレイブック（trip-matomeblog方式）

バージョン：v1.7
最終更新：2026年8月12日
管理者：HEADチャット
正本配置先：C:\headquarters\shared-multi-site-playbook.md

このドキュメントは、trip-matomeblog.comで確立した自動投稿ブログの仕組みを、新しいサイトに展開する際の手順書です。上から順に進めれば、新サイトを構築できることを目指しています。

## 目次

1. サイト立ち上げチェックリスト
2. そのまま移植できる型
3. サイトごとに再設計が必要な部分
4. 設計判断集
5. trip-matomeblogで得た教訓
6. ドキュメント修正ルール
7. 更新履歴

## 1. サイト立ち上げチェックリスト

### ステップ1：基盤構築
- ドメイン取得
- WordPress設置
- Application Password発行（WP管理画面 > ユーザー > プロフィール）
- GitHubリポジトリ作成（trip-blogをテンプレートとしてコピー、または git clone後に .git履歴を削除して新規リポジトリ化。②の方法を選ぶ場合、削除前に過去コミット履歴にAPIキー等の機微情報が含まれていないか確認すること）
- GitHub Secrets設定（ANTHROPIC_API_KEY, PIXABAY_API_KEY, WP_BASE_URL, WP_USERNAME, WP_APP_PASSWORD, WP_CATEGORY_IDS, GOOGLE_SERVICE_ACCOUNT_JSON）
- permissions: contents: writeを全ワークフローファイルに明記
- WP_POST_STATUSがpublishになっているか確認
- WordPress投稿タイムアウトを60秒に設定

### ステップ2：サイト設計
- HEADチャットでコンセプト・方針確定
- サイトコンセプト・キャッチフレーズを決める
- 読者ターゲット（年代・ペルソナ）を定義する
- カテゴリー構成を決める
- criteria.jsonを新サイト用に作り直す
- RSSフィードを収集する（feeds.yaml）
- アフィリエイト提携先を選定・申請する

### ステップ3：コード側のカスタマイズ
- summarizer.py・article_generator.pyのSYSTEM_PROMPTを書き換える
- WordPressカテゴリーIDを取得し、CATEGORY_MAPを更新する
- affiliate.pyの提携先リンクを新サイト用に差し替える
- タイトル形式を【○○！】形式に統一する
- travel/product記事で画像検索ロジックを分岐させる
- facility_name抽出プロンプトに「一般名詞禁止・固有名詞のみ」を明記する
- image_queryの固有名詞禁止ルールをプロンプトに明記する（国名・地域名も禁止）
- SEOキーワードの本文反映ルールをプロンプトに明記する

### ステップ4：テスト運用
- ローカルで1〜2記事分のテスト実行
- draft設定で1回実行し、記事の質を確認
- 問題なければpublish設定に変更
- Google Cloud ConsoleでIndexing APIが有効化されているか確認
- HEADチャットに稼働報告

## 2. そのまま移植できる型

画像取得パイプライン全体（images.py）：関連性チェック・ブラックリスト・履歴管理の3層構造
3系統の投稿アーキテクチャ（main.py等）：RSS自動投稿／体験記事／テーマ連動記事
images.pyの一元化設計原則：呼び出し元インターフェースを変えず、画像仕様変更はimages.py単体で完結
criteria.json/criteria.py分離パターン：判定基準のデータとロジックの分離。中身はサイトごとに再設計するが構造は共通
categoryのLiteral型強制（summarizer.py）：AIの出力を型レベルで強制し、想定外の値を返す不具合を防ぐ
image_queryの1語ルール：画像検索キーワードは1語厳守、固有名詞・抽象語禁止、コード側でも.split()[0]で強制
image_queryの固有名詞禁止ルール：固有名詞は検索ヒット数が極端に少ないため一般名詞に変換する。国名・地域名（フィリピン・タイ・ハワイ・北海道・沖縄等）も単独1語であっても禁止
アフィリエイトリンク管理手順：ASPでURL取得→WP Redirectionで/go/xxx登録→コードに定数追加
体験記事のシリーズ相互リンク機構（article_generator.py）：related_urlsによるトリガー方式
related_urlsの実仕様：複数指定時の区切り文字は半角パイプ「|」、URL形式は相対パス「/?p=数字」。投稿順：①は空欄、②は①のみ指定、③以降は全ての前記事を指定
travel/product記事の画像検索分岐パターン：travel記事はPixabayに「日本」プレフィックスを追加、product記事は除外。product記事専用フォールバックキーワードリスト（PRODUCT_FALLBACK_KEYWORDS）を別途定義する
facility_name抽出ロジックの型：施設名は固有名詞のみ返させる。一般名詞はプロンプトで明示的に禁止する
HTML文字列置換の安全パターン（wordpress.py）：置換はテキストノードのみに限定し、imgタグのalt等を破壊しないよう対象範囲を明示的に限定する
own_photos優先取得の原則：own_photosがある場合は自動取得より優先する。不足分のみAPIで補完する。特定の他施設・他地域と判明しているものは使わない。場所を特定できない汎用写真は記事のイメージに合えば使用可
own_photo_files枚数上限ガード：IMAGES_COUNTを超える指定があった場合、自動で先頭N枚に切り詰めるガードを実装する
タイトル【○○！】形式統一：全系統でタイトル形式を統一する
WordPress投稿タイムアウト60秒（wordpress.py）：create_post関数のタイムアウトは60秒を標準とする
画像関連性判定の危険検出パターン：判定基準を3パターンの危険検出のみに絞り込む。①人物写真の感情ミスマッチ②個人・団体・国・宗教をおとしめる組み合わせ③医療・健康・お金の誤情報
記事主題の関連性判定への反映（article_topic引数）：filter_relevant()にarticle_topic引数を追加し、記事タイトルを判定プロンプトに渡す。strict=False時に記事主題と画像が明らかにかけ離れている場合は除外する旨を明記する
SEOキーワードの本文反映ルール：文体を崩さず、最低1個のキーワードを文字列としてそのまま本文に含める。「無理なく盛り込む」ではなく「最低1個は文字列としてそのまま含める」という強制力のある指示にする。完全自動系統は事後チェック運用、手動系統は投稿前に候補を提示してから実行する
X投稿文・note本文へのURL自動追記：投稿完了後の確定IDを使いブログ記事URLを自動追記する。GitHub ActionsログではSecretsマスキングによりURL表示が***になるため手動確認が必要
画像ソース（source）の引き継ぎ（wordpress.py）：upload_photo_to_media()でWordPressに再アップロードする際、元のPhotoオブジェクトのsourceフィールド（pixabay/wikimedia等）を必ず引き継ぐこと。引き継がないとデフォルト値にリセットされ、クレジット表記が誤った値になる
strict引数のプロンプト反映（images.py）：RelevanceChecker.filter_relevant()のstrict引数（メイン検索=True／フォールバック=False）は必ずプロンプトに反映すること。strict=True時のみ「看板のみの画像」「室内・棚のクローズアップ」「無関係な食べ物単体」を明示的な除外基準として追加する。strict=False時は基本方針（多少ズレてもOK）を維持し画像0件を防ぐ

## 3. サイトごとに再設計が必要な部分

RSSフィード構成：新サイトのジャンルに合うフィードを収集（4.3参照）
WordPressカテゴリー定義：新サイトのテーマに合わせて設計（4.1参照）
SYSTEM_PROMPT（文体・ターゲット読者）：summarizer.py・article_generator.py
criteria.jsonの年代・価格帯・OK/NGリスト：読者ターゲットに合わせて再設計（4.2参照）
アフィリエイトリンクの提携先：サイトテーマにより変わる
ブランディングコンセプト：コアフレーズ、金銭感覚基準等
facility_nameの定義：サイトテーマによって異なる（旅行→宿泊施設、終活→老人ホーム・葬儀社等）
Pixabay「日本」プレフィックスの適用範囲：サイトのジャンルに応じて判断する
PRODUCT_FALLBACK_KEYWORDSの内容：サイトのテーマに合わせて再設計する
SEOキーワードの生成方法：自動生成（daily記事）か手動入力（体験記事のnote欄）かはサイトの投稿系統に応じて設計する

## 4. 設計判断集

### 4.1 カテゴリー数・カテゴリー名の設計指針

現状（trip-matomeblog）：9個・固定
要検討：設計思想が言語化されていない。次サイト構築時に以下を整理する：
- カテゴリーはRSSフィードのジャンル分布と一致させるべきか、読者の興味軸で独自に作るべきか
- 1カテゴリーあたりの想定記事数
- カテゴリーとアフィリエイト提携先の対応関係

### 4.2 読者ターゲット・criteria.jsonの設計

現状（trip-matomeblog）：40代後半〜65歳（メインは50代）、価格帯宿泊1.5〜5万円/泊・食事〜1万円程度
確定した型：criteria.json（データ）とcriteria.py（ロジック）を分離する構造は移植可能。中身はサイトごとに再設計。
要検討：新サイトのターゲットを決める際のチェックリストが未整備。最低限決めるべき：
- 対象年代
- 価格帯目安
- 歓迎する体験・歓迎しない体験の具体例リスト
- フォールバック原則（3原則程度に集約）

### 4.3 RSSフィード選定の基準

要検討：体系化されていない。経験則として得ていること：
- 更新頻度が低すぎるフィードは候補プールに寄与しない
- 記事の質は運用しながら除外していく形になりやすい
- フィード数の目安：trip-matomeblogでは16〜19本程度で運用中

### 4.4 画像取得の方式（無料API vs 有料ストックフォト）

現状：Wikimedia Commons API + Pixabay API（無料）を使用。

確定した判断：
- 写真ACは自動取得が不可能なため不採用
- Pixabayには有料プランがなく、商用利用は無料プランのみ
- Unsplash・Wikimediaの「有料版」は検討対象外
- Shutterstockは個人向けプランにAPIなし、Premierライセンス（大企業向け）が必要→不採用
- PIXTAは公式API制度あり、規約も比較的整理されている（最有力候補・問い合わせ中）
- イメージマート（Aflo系）：カスタムプラン内でAPI提供の記載を確認（問い合わせ中）

調査中（2026/07/16時点）：PIXTA・イメージマート・Afloへの正式問い合わせ送付済み・回答待ち。

無料APIの構造的限界：「温泉」等のニッチな日本語キーワードで0〜1枚しか採れないケースが継続発生（発生率20%）。有料サービス導入後に再検討する。

## 5. trip-matomeblogで得た教訓

### 5.1 AIへの「お願い」より型による強制
教訓：プロンプトでの指示は「お願い」でしかなく、AIが従わないことがある。可能な箇所は型・コードで強制する。

### 5.2 画像検索キーワードは短ければ短いほど良い
実測：「フェリー」1語で17万件、「フェリー 船旅」2語で1件。
教訓：検索キーワードは具体的な名詞1語が基本。コード側でも.split()[0]のような強制ロジックを二重に用意する。

### 5.3 ブラックリスト・除外ロジックは「理由付きで明示的に」
教訓：判定結果は理由付きで明示的に返させ、「単に関連性が低いだけ」と「明確に危険/低品質」を区別するロジックをコード側に持たせる。

### 5.4 コスト管理は「本番実行」と「開発中のテスト実行」を区別する
実例：1日で$5→$9超消費。
教訓：コード変更の確認はまずローカルで軽量テストを行い、本番ワークフロー全体の実行は最終確認の1回に絞る。

### 5.5 コマンド実行・ファイル編集の事故防止
教訓：複数行のコードは必ず.pyファイル等として保存してから実行する。長文ファイルはダウンロードして配置する方法が確実。

### 5.6 決済・アカウント管理
決済トラブル発生：2026/07/07〜2026/07/12解決。
教訓：クレジットカードはデビットカードではなく通常のクレジットカードを用意する。決済トラブル発生時は複数の決済経路を試す。

### 5.7 WordPress投稿タイムアウトは60秒を標準とする
教訓：wordpress.pyのcreate_post関数のタイムアウト値は60秒を標準とする。

### 5.8 ワークフローの投稿ステータスは必ず確認する
教訓：トラブル対応後は必ず設定を元に戻したかを確認する。WP_POST_STATUS=publishを確認すること。

### 5.9 Google Cloud ConsoleのAPIは明示的に有効化する
教訓：Google系APIを使う場合は、APIが有効化されているかを必ず確認する。サービスアカウント作成とAPI有効化は別の操作。

### 5.10 文字列置換は対象範囲を限定する
教訓：文字列置換はテキストノードのみに限定し、HTML属性値を誤って書き換えないよう対象範囲を明示的に限定する。

### 5.11 facility_nameには固有名詞のみを返させる
教訓：facility_name抽出プロンプトには「固有名詞のみ返すこと。一般名詞は返さないこと」を明示する。抽出できない場合はnullを返させる。

### 5.12 プロンプトの指示は具体的・強制力のある表現にする
事例：「無理なく盛り込む」では効果が出なかった。「最低1個は文字列としてそのまま含める」に変えて初めて効果が出た。
教訓：「〜するよう心がける」等の任意表現はAIに無視されやすい。「必ず〜する」「最低〜個は〜する」という強制力のある表現を使う。

### 5.13 確認は必ず「実際のファイルの中身」に対して行う
教訓：「〜のはず」という前提で進めず、実際のファイルをSelect-String等で確認してから作業する。

### 5.14 image_queryに国名・地域名を使うと無関係な画像がヒットする
事例：「フィリピン」というimage_queryで、航空路線再開記事にフィリピン紙幣の写真が使われた。
教訓：image_queryには国名・地域名も禁止する。具体例（「フィリピン行きの航空便」記事→「飛行機」）もプロンプトに示す。

### 5.15 product記事とtravel記事でフォールバック検索キーワードを分ける
事例：EENOURポータブル電源記事（product）に旅館看板写真がヒットした。
教訓：fetch_related_photos()のフォールバックキーワードはarticle_typeで分岐させる。PRODUCT_FALLBACK_KEYWORDSを定義して渡す。

### 5.16 関連性判定には記事の主題（article_topic）を渡す
事例：strict=False時にキーワード単体一致だけで判定し、記事主題と無関係な画像が通過した。
教訓：filter_relevant()にarticle_topic引数を追加し、記事タイトルを判定プロンプトの冒頭に渡す。

### 5.17 画像ソースはWordPress再アップロード時に必ず引き継ぐ
事例：upload_photo_to_media()でPhotoオブジェクトを再生成する際にsourceフィールドを引き継がず、デフォルト値「pixabay」にリセットされてWikimedia由来の画像に「on Pixabay」と誤ったクレジット表記が付いた。
教訓：WordPressへの再アップロード後に新しいPhotoオブジェクトを生成する場合は、source=photo.sourceのように元のsource値を明示的に引き継ぐこと。

### 5.18 strict引数は必ずプロンプトに反映する
事例：RelevanceChecker.filter_relevant()のstrict引数がプロンプトに反映されておらず、メイン検索（strict=True）でも常に緩い基準で判定していた。看板・室内・無関係な食べ物単体の画像が通過した。
教訓：strict=True時とstrict=False時で異なる除外基準をプロンプトに明記する。strict=True時の除外基準（看板のみ・室内棚のクローズアップ・無関係な食べ物単体等）を具体例として示す。strict=False時は画像0件を防ぐため基本方針（多少ズレてもOK）を維持する。

### 5.21 画像プール方式では収集フロー自体にURL記録ステップを組み込む
事例：画像プール方式（事前収集・ローカル保存）で、ファイル保存後に元URL（Unsplash/Pixabay等の出典）を遡って追跡しようとしたが非現実的だった。
教訓：ファイル保存前に元URLを必ず記録すること。収集スクリプトの設計段階で「保存」と「URL記録」を1つのステップとして組み込む。後から追加しようとしても手遅れになりやすい。

### 5.22 Literal型の選択肢は辞書から動的生成し二重管理を避ける
事例：Pydanticスキーマでcategory等の選択肢を、辞書（CATEGORY_MAP等）とLiteral型の両方に書いており、片方だけ更新すると不整合が起きる状態だった。
教訓：`Literal[tuple(辞書.keys())]`のように辞書から動的生成し、選択肢の定義箇所を1つに集約する。oyako-shukatsu.jpのsummarizer.pyで採用。他サイトへの遡及適用は各サイトの判断で。

### 5.19 「想像ベース」で生成するテキストは、実データと食い違う事故を起こす
背景：alt文（eyecatch_alt・related_alt_1・related_alt_2）は記事生成AIが記事内容だけを見て想像で書いていたが、画像取得は別ロジック（fetch_related_photos()）が独立して行っていたため、両者が実物照合されずにペアリングされていた。フォールバック検索でズレた画像が採用された場合、alt文だけもっともらしい説明が付き、画像とテキストが矛盾する事故（ドーハ空港記事に日本の旅館室内写真等）が発生した。
教訓：AIに「実際にはどれが採用されるか分からない段階」で説明文を書かせる設計は避ける。可能な場合は、実データ（採用された画像のファイル名・検索に使った実キーワード等）が確定してから、それを元に機械的または再生成的にテキストを作る工程を挟む。

### 5.20 個別バグ対応が続いたら、共通する構造的原因がないか棚卸しする
背景：画像関連性判定の甘さ、ブラックリスト非永続化、フォールバックキーワードの偏り、alt文の想像ベース生成は、一見別々の不具合に見えるが、根っこは「AIの想像・一般論ベースの生成」対「実データとの照合不足」という共通の構造的原因を持つ。
教訓：個別バグを次々に潰していく局面が続いたら、都度の対症療法で終わらせず、一連の不具合に共通するパターンがないか棚卸しするタイミングを設ける。

### 5.23 「確認テスト合格」は免除券ではない
背景：2026/08/09、旅行チャットがHEAD確認テストに合格した直後、own_photos関連の体験記事作業（次シリーズの話題）が会話に出た際、事前確認ルール（run_experience_article.pyの該当関数を読む）を実行せずに次の判断に進もうとした事故が発生した。
教訓：引き継ぎ時の確認テストに合格したことは、その時点の理解を示すだけであり、以降の作業で再確認を省略してよいことを意味しない。特にリスクの高い操作（ファイルの自動移動・削除等を伴う仕様）は、実際に着手する前に、都度該当コード・ドキュメントを読み直すこと。

### 5.24 ルールの発動条件は「着手時」ではなく「話題に出た時点」
背景：上記事故の原因分析より。「作業に着手する前に確認する」というルールは、着手のタイミングを甘く判断すると機能しない。
教訓：関連する話題が会話に出た時点で、実際に着手するかどうかに関わらず確認行動を取ること。「まだ着手していないから後でいい」という判断がルールをすり抜ける典型パターンであると認識する。

### 5.25 警告だけで止まらない
背景：上記事故で、チャットは「まだ確認していません、どう進めますか」という警告は出したが、確認する行動そのものは保留してしまった。
教訓：ルールが発動したら、一言断った上でその場で確認（軽量コマンド等）まで進め、判断を仰ぐのは確認した後にする。

### 5.26 レンタルサーバーのWAFがAPI経由の書き込み（POST/DELETE）をブロックする可能性がある
事例：終活サイトのWordPressメディア一括アップロードで、API経由（wp-json）のPOSTが一部403エラーで失敗し続けた。原因はレンタルサーバーのセキュリティ機能（WAF）が、プログラム経由のPOST/DELETEリクエストを一律ブロックしていた可能性が高い。GET（読み取り）は問題なく通過。ブラウザ経由の手動アップロードは同じファイルで成功することを確認。
教訓：新しいレンタルサーバー環境でAPI経由の書き込み処理（投稿・アップロード・削除）を設計する際は、事前に軽量テストでPOST/DELETEの可否を確認すること。ブロックされる場合、手動アップロードでの代替、または該当WAF設定の緩和をサーバー管理画面で確認する。

## 6. ドキュメント修正ルール

### 6.1 修正の種類と担当者

サイト固有の修正（例：RSSソース変更・カテゴリー追加）
担当：各サイトチャット
手順：自サイトのspec.mdを修正→HEADチャットに報告

共通ルールに影響する修正（例：images.pyの仕様変更・新しい教訓の追加）
担当：HEADチャット
手順：各サイトチャットから報告→HEADチャットが本書を修正→全サイトに反映指示

重要：共通ファイルの変更は必ず事前にHEADチャットの承認を得てから実施すること。

### 6.2 修正フロー

各サイトで修正発生
→ サイト固有？→ YES：各サイトチャットがspec.mdを修正→HEADチャットに報告
→ 共通事項？→ HEADチャットに報告・判断を仰ぐ（修正前に承認を得る）
→ HEADチャットがshared-playbookを修正・バージョンアップ
→ 全サイトチャットに反映指示
→ 各サイトチャットが自サイトのspec.md・実装を更新

### 6.3 判断基準

そのサイトだけの話 → 各サイトチャットが修正
複数サイトに影響する話 → HEADチャットが修正
迷ったら → HEADチャットに報告して判断を仰ぐ

### 6.4 バージョン管理ルール

バージョンはvX.Y形式で管理する
- X（メジャー）：構造・方針の大きな変更時
- Y（マイナー）：教訓追加・細かいルール追記時
各サイトのspec.mdの末尾に参照バージョンを明記する
例：参照中：shared-multi-site-playbook.md v1.3
バージョンが上がった節目のみ全サイトに複製・反映する

### 6.5 チャット間連携ルール

HEAD経由が必要なケース：
- 共通ファイルの変更を伴う連携
- 方針判断が必要な連携
- サイト間での設計統一が必要な連携

直接連携でOKなケース：
- 既存ドキュメントの内容確認・案内
- 実装フェーズ中の技術的な質問への回答
- 共通ファイルの変更を伴わない情報共有

### 6.6 更新時の手順

1. HEADチャットでshared-playbookを修正
2. バージョン番号・更新履歴を追記
3. git add → commit → push（C:\headquarters）
4. 各サイトチャットに通知
5. 各サイトチャットが自サイトのspec.md末尾の参照バージョンを更新

## 7. 更新履歴

### v1.7（2026/08/12）
- 5章「教訓」に5.19・5.20を復元（trip-blogリポジトリへの誤コミットにより一時的に失われていたものを復旧）
- 5章「教訓」に5.23〜5.26を追加：
  - 「確認テスト合格」は免除券ではない（5.23）
  - ルールの発動条件は「着手時」ではなく「話題に出た時点」（5.24）
  - 警告だけで止まらない（5.25）
  - レンタルサーバーのWAFがAPI経由の書き込みをブロックする可能性がある（5.26、終活チャットからの報告）
- 参照バージョン：README-document-management.md v1.1

### v1.6（2026/08/04）
- 5.21 画像プール方式では収集フロー自体にURL記録ステップを組み込む を追加
- 5.22 Literal型の選択肢は辞書から動的生成し二重管理を避ける を追加
- 参照バージョン：README-document-management.md v1.1

### v1.4（2026/07/21）
- 2章「移植可能な型」に以下を追加：
  - 画像ソース（source）の引き継ぎ（wordpress.py）
  - strict引数のプロンプト反映（images.py）
- 5章「教訓」に5.17・5.18を追加：
  - 画像ソースはWordPress再アップロード時に引き継ぐ（5.17）
  - strict引数は必ずプロンプトに反映する（5.18）
- 参照バージョン：README-document-management.md v1.1

### v1.3（2026/07/16）
- 2章「移植可能な型」に以下を追加：
  - image_queryの固有名詞禁止ルールに国名・地域名も禁止を明記
  - related_urlsの実仕様（パイプ|区切り・相対パス・投稿順のルール）
  - travel/product記事の画像検索分岐パターンにPRODUCT_FALLBACK_KEYWORDSを追加
  - own_photosの汎用素材使い回しルール
  - own_photo_files枚数上限ガード
  - 画像関連性判定の危険検出パターン（3パターンに絞り込み）
  - 記事主題の関連性判定への反映（article_topic引数）
  - SEOキーワードの本文反映ルール
  - X投稿文・note本文へのURL自動追記
- 3章にPRODUCT_FALLBACK_KEYWORDSの内容・SEOキーワード生成方法を追加
- 4.4節を更新：Shutterstock不採用・PIXTA最有力・問い合わせ状況を反映
- 5章に5.12〜5.16を追加
- 6章に6.5「チャット間連携ルール」を新規追加
- 6.1節に「共通ファイルの変更は必ず事前承認を得てから実施する」を明記
- 参照バージョン：README-document-management.md v1.1

### v1.2（2026/07/12）
- 2章に7項目追加
- 5章に5.7〜5.11を追加
- 4.4節を全面更新
- 参照バージョン：README-document-management.md v1.0

### v1.1（2026/07/10）
- 6章「ドキュメント修正ルール」を新規追加
- 参照バージョン：README-document-management.md v1.0

### v1.0（2026/07/08）
- 初版作成
